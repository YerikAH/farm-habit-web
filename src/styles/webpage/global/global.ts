import { css } from 'styled-components'

export const HeadlinePrincipalGlobal = css`
  font: normal normal 700 1.5rem/2rem var(--font-primary);
  color: var(--black-100);
  text-align: center;
`
export const TextBodyOneGlobal = css`
  font: normal normal 400 0.8rem/1rem var(--font-primary);
  color: var(--black-70);
  text-align: center;
  margin-top: 2rem;
`
export const SectionCenterGlobal = css`
  width: 100%;
  max-width: 1200px;
  display: grid;
  place-items: center;
`